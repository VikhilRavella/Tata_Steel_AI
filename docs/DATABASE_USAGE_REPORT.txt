==================================================
DATABASE USAGE REPORT
==================================================

ACTIVE DATABASE IN FINAL SUBMISSION:
sqlite:///./backend/maintenance.db

REASONING:
The `backend/database.py` defaults to `os.getenv('DATABASE_URL', 'sqlite:///./backend/maintenance.db')`.
Because the `.env` file was omitted from the final package (only `.env.example` remains), the application correctly defaults to the fallback DB.
This `maintenance.db` was safely copied with the `backend/` directory and contains all necessary demo data.

UNUSED DATABASES (Can be safely ignored/deleted):
- app.db
- backend.db
- database.db
